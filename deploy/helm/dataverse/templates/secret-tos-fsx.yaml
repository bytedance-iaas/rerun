# 已退役(2026-08-19 云产品化):这里原来渲染 fsx CSI(TOS 模式)挂载凭证的
# Secret(AccessKeyId / SecretAccessKey)。质检台不再静态挂载任何 TOS 桶 ——
# 数据面改为 TOS SDK 直连,凭证以 TOS_ACCESS_KEY / TOS_SECRET_KEY 环境变量
# 注入(复用 viewer-secrets 里的同一对 AK/SK,见 daft-curation.yaml),
# 不再需要为 CSI 单独渲染一份密钥。文件保留为注记,不渲染任何对象。
