# Retired (2026-08-19, cloud productization): this used to render the Secret
# holding the fsx CSI (TOS mode) mount credentials (AccessKeyId /
# SecretAccessKey). The curation console no longer statically mounts any TOS
# bucket — its data plane talks to TOS through the SDK, with the credentials
# injected as TOS_ACCESS_KEY / TOS_SECRET_KEY (the same AK/SK pair from
# viewer-secrets, see daft-curation.yaml), so there is nothing left to render
# for the CSI. Kept as a note; renders no object.
