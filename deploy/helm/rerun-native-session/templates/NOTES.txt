native viewer 会话 {{ include "rerun-native-session.name" . }} 已启动。

{{- if .Values.ingress.enabled }}

访问(公网,经共享 APIG 网关):
  域名在 APIG 控制台查(host = {{ include "rerun-native-session.host" . }}):
    https://console.volcengine.com/veapig → 服务列表
  浏览器打开 https://<域名>/vnc.html?autoconnect=true&resize=remote
  连接时输入会话密码。
{{- else }}

访问(port-forward):
  kubectl -n {{ .Release.Namespace }} port-forward pod/{{ include "rerun-native-session.name" . }} 9092:8080
  浏览器打开 http://127.0.0.1:9092/vnc.html?autoconnect=true&resize=scale
{{- end }}

用完删掉(节点资源按运行计费,别忘了):
  helm uninstall {{ .Release.Name }} -n {{ .Release.Namespace }}
