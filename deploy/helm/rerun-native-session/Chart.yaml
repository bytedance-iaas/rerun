apiVersion: v2
name: rerun-native-session
description: >-
  A self-service, on-demand native viewer session in the cloud
  (one release per person, uninstall when done — one pod per person, deleted when done).
type: application
version: 0.1.0
appVersion: "0.36.0"
